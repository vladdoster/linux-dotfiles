#!/bin/sh

system_wallpaper="/home/vlad/.config/assets/wallpaper.png"

[ ! -z "$1" ] && cp "$1" "$system_wallpaper" \
              && notify-send -i "$system_wallpaper" "Wallpaper changed."
xwallpaper --focus "$system_wallpaper"
