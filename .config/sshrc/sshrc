#! /bin/bash

#-- correct username --# 
if [ "$USER" != "$(whoami)" ]; then
  USER=$(whoami)
fi

#-- sshrc --#
export SSHRC_DIR="$SSHHOME/sshrc.d"
source "$SSHRC_DIR"/.git-completion.bash
source "$SSHRC_DIR"/.git-prompt.bash
source $SSHRC_DIR/.bash_profile
source $SSHRC_DIR/.bashrc
